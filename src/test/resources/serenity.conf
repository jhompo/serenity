webdriver {
    driver = chrome
    chrome {
        options = [
            "--remote-allow-origins=*",
            "--headless",
            "--disable-gpu",
            "--no-sandbox",
            "--disable-dev-shm-usage"
        ]
    }
}
